# aidd-personal-dc

Personal AI-driven-development projects.

## Contents

- [`pr-review-agent/`](./pr-review-agent/) — Daily AI code-review agent for open
  scraper PRs. Local cron via `launchd` + headless `claude -p`. Writes one
  markdown review per PR to `~/pr-reviews/`.
